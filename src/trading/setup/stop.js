'use strict';
function finite(v){return Number.isFinite(Number(v))&&Number(v)>0;}
function calculateStop({direction,entry,structures}){if(!finite(entry))return {valid:false,reason:'Valid entry price is required'};const candidates=direction==='LONG'?[['30m',structures?.['30m']?.protectedLow],['1h',structures?.['1h']?.protectedLow]]:[['30m',structures?.['30m']?.protectedHigh],['1h',structures?.['1h']?.protectedHigh]];for(const [timeframe,point] of candidates){const level=Number(point?.price??point);if(!finite(level))continue;if(direction==='LONG'&&level<Number(entry))return {valid:true,direction,stop:level,reference:{timeframe,structuralLevel:level},reason:`LONG stop at protected ${timeframe} low`};if(direction==='SHORT'&&level>Number(entry))return {valid:true,direction,stop:level,reference:{timeframe,structuralLevel:level},reason:`SHORT stop at protected ${timeframe} high`};}return {valid:false,direction,stop:null,reason:`No protected structural ${direction==='LONG'?'low':'high'} exists on the correct side of entry`};}
module.exports={calculateStop};
